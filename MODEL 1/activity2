#activity2
class vehicle:
    def _int_ (self,max_speed,milage):

        self.maxspeed = max_speed
        self.millage = milage

modelX = vehicle(240,18)
print("modelX max speed is",modelX.max_speed)
print("modalX milage is",modelX.millage)
    